#/bin/bash
WORKSPACE=2
PROJECT=~/dev/projects/ridehealth
i3-msg "workspace $WORKSPACE; append_layout ~/.i3/workspace.json"
i3-msg "workspace $WORKSPACE; exec code-oss -w $PROJECT;"
i3-msg "workspace $WORKSPACE; exec urxvt -name code -e /usr/sbin/zsh -is eval 'cd $PROJECT';"
i3-msg "workspace $WORKSPACE; exec urxvt -name code -e /usr/sbin/zsh -is eval 'cd $PROJECT';"
sleep 3

WORKSPACE=3
PROJECT=~/dev/projects/kora
i3-msg "workspace $WORKSPACE; append_layout ~/.i3/code.json"
i3-msg "workspace $WORKSPACE; exec code-oss -w $PROJECT;"
i3-msg "workspace $WORKSPACE; exec urxvt -name code -e /usr/sbin/zsh -is eval 'cd $PROJECT';"
i3-msg "workspace $WORKSPACE; exec urxvt -name code -e /usr/sbin/zsh -is eval 'cd $PROJECT';"
sleep 3

WORKSPACE=4
PROJECT=~/dev/projects/phocus
i3-msg "workspace $WORKSPACE; append_layout ~/.i3/code.json"
i3-msg "workspace $WORKSPACE; exec code-oss -w $PROJECT;"
i3-msg "workspace $WORKSPACE; exec urxvt -name code -e /usr/sbin/zsh -is eval 'cd $PROJECT';"
i3-msg "workspace $WORKSPACE; exec urxvt -name code -e /usr/sbin/zsh -is eval 'cd $PROJECT';"
sleep 3

WORKSPACE=5
PROJECT=~/dev/projects/astrum
i3-msg "workspace $WORKSPACE; append_layout ~/.i3/code.json"
i3-msg "workspace $WORKSPACE; exec code-oss -w $PROJECT;"
i3-msg "workspace $WORKSPACE; exec urxvt -name code -e /usr/sbin/zsh -is eval 'cd $PROJECT';"
i3-msg "workspace $WORKSPACE; exec urxvt -name code -e /usr/sbin/zsh -is eval 'cd $PROJECT';"
sleep 3

WORKSPACE=6
PROJECT=~/dev/projects/coinbits
i3-msg "workspace $WORKSPACE; append_layout ~/.i3/code.json"
i3-msg "workspace $WORKSPACE; exec code-oss -w $PROJECT;"
i3-msg "workspace $WORKSPACE; exec urxvt -name code -e /usr/sbin/zsh -is eval 'cd $PROJECT';"
i3-msg "workspace $WORKSPACE; exec urxvt -name code -e /usr/sbin/zsh -is eval 'cd $PROJECT';"
sleep 3

WORKSPACE=7
PROJECT=~/dev/go/src/github.com/the-infatuation
i3-msg "workspace $WORKSPACE; append_layout ~/.i3/code.json"
i3-msg "workspace $WORKSPACE; exec code-oss -w $PROJECT;"
i3-msg "workspace $WORKSPACE; exec urxvt -name code -e /usr/sbin/zsh -is eval 'cd $PROJECT';"
i3-msg "workspace $WORKSPACE; exec urxvt -name code -e /usr/sbin/zsh -is eval 'cd $PROJECT';"
sleep 3

WORKSPACE=8
PROJECT=~/dev/projects/clank
i3-msg "workspace $WORKSPACE; append_layout ~/.i3/code.json"
i3-msg "workspace $WORKSPACE; exec code-oss -w $PROJECT;"
i3-msg "workspace $WORKSPACE; exec urxvt -name code -e /usr/sbin/zsh -is eval 'cd $PROJECT';"
i3-msg "workspace $WORKSPACE; exec urxvt -name code -e /usr/sbin/zsh -is eval 'cd $PROJECT';"
sleep 3
